[project]
name = "bom-forecast"
version = "0.1.0"
description = "Fetch BOM weather forecasts and convert to CSV"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
